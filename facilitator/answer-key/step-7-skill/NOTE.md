# Step 7 — skill that writes back (the wiring rule)

Two outputs should exist after this step.

**1. The call-prep brief** in `work/call-prep_next-24h.md` — one page per external call in the next 24h
(from `brain/calendar_next-24h.ics`: the Clay sponsorship call and the Sam Jacobs / Pavilion keynote
call). This is the skill's normal output. Format is defined in `skills/call-prep/SKILL.md`.

**2. The write-back** — new Timeline lines on the entity pages the prep touched. THIS is the lesson: a
run that surfaces a signal must also update the brain, or the brain stops compounding from that pathway.

Expected additions (dates will match the participant's run day):

On `brain/companies/clay.md`, below the fold:
```
- 2026-06-2x — Call-prep generated ahead of the sponsorship call; co-creation proposal still pending [Source: work/call-prep_next-24h.md]
```

On `brain/people/bruno-estrella.md`, below the fold:
```
- 2026-06-2x — Surfaced in call-prep as the first contact; proposal + 2 dates owed [Source: work/call-prep_next-24h.md]
```

And a `brain/log.md` line:
```
## [2026-06-2x] enrich | call-prep wrote back to Clay, Bruno Estrella
```

**Grading check:** if the participant only produced `work/call-prep_next-24h.md` and nothing changed in
`brain/`, the wiring rule was not applied — that's the exact failure the step is teaching against.

> Where this sits in gbrain: `call-prep` and `sponsor-outreach` are **Tier 2 orchestration** skills.
> Data-source skills (Tier 1) and pipeline skills (Tier 3) are gbrain's future surface. The rule that
> matters today: every pathway that touches a person/company writes their page back.
